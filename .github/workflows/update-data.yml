name: Update Market Data

on:
  schedule:
    - cron: '0 0,4,10,14 * * *'
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:                          # 新增这一行
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'   # 新增这一行

    steps:
    - name: Checkout
      uses: actions/checkout@v4
    ...
